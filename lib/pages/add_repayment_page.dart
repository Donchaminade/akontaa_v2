import 'package:flutter/material.dart';

class AddRepaymentPage extends StatelessWidget {
  const AddRepaymentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ajouter un remboursement'),
      ),
      body: const Center(
        child: Text('Page d\'ajout de remboursement - à implémenter'),
      ),
    );
  }
}
